For each depth we need to iterate over the columns and rotate 4 items
Same as LC 48 but trying to user recursive function just to avoid too many lines of code

Without recursion we can reduce the space complexity because we only swap values